const { User, Member } = require("./UserData")
const { BaseMessage } = require("./Message")
const { TeksChannel, DirektoriChannel } = require("./Channel")
const { BaseGuild } = require("./Guilds")


module.exports = {
    User,
    Member,
    BaseMessage,
    TeksChannel,
    DirektoriChannel,
    BaseGuild
}